# Dependencies
node_modules
yarn.lock
pnpm-lock.yaml

# Project configuration (user-specific, not committed)
node9.config.json

# Build Artifacts (Never commit these!)
dist
coverage

# Environment & Secrets (CRITICAL)
.env
.env.local
.env.development
.env.test
env
credentials.json

# Local IDE / tool settings (machine-specific)
.claude/
.cursor/
.vscode/settings.json
*.local

# Claude Code working notes — file stays in repo for now (already tracked),
# but local edits are skip-worktree'd so they don't accidentally commit.
# To re-track: git update-index --no-skip-worktree CLAUDE.md
CLAUDE.md

# OS Junk
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
